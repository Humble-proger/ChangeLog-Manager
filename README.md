# CHANGELOG Manager 📝

Утилита для управления файлом изменений (CHANGELOG) в проектах. Поддерживает формат [Keep a Changelog](https://keepachangelog.com/) и автоматизирует процесс ведения истории изменений.

## ✨ Особенности

- ✅ **Простота использования** - интуитивные команды CLI
- ✅ **Структурированное хранение** - JSON для невыпущенных изменений
- ✅ **Настраиваемые пути** - гибкая конфигурация расположения файлов
- ✅ **Автоматизация релизов** - создание версий одной командой
- ✅ **Интеграция с Git** - автоматическое создание тегов
- ✅ **Поддержка нескольких проектов** - работа с разными проектами одновременно
- ✅ **Резервное копирование** - автоматические backup'ы изменений

## 📦 Установка

### Способ 1: Установка из исходников

```bash
# 1. Скачайте скрипт
git clone <репозиторий>
cd chlog

# 2. Сделайте скрипт исполняемым
chmod +x changelog.py

# 3. Создайте симлинк в /usr/local/bin
sudo ln -s $(pwd)/changelog.py /usr/local/bin/chlog

# 4. Проверьте установку
chlog --help
```

### Способ 2: Установка через pip (для разработчиков Python)

```bash
# Установите зависимости
pip install -r requirements.txt

# Или установите напрямую
pip install .
```

## 🚀 Быстрый старт

### Инициализация проекта

```bash
# Перейдите в папку вашего проекта
cd /path/to/your/project

# Инициализируйте CHANGELOG
chlog init --name "Мой Проект"
```

### Добавление изменений

```bash
# Добавить новую функцию
chlog add added "Реализована система авторизации"

# Добавить исправление с указанием автора
chlog add fixed "Исправлена утечка памяти" --author "Иван Петров"

# Добавить изменение с несколькими типами
chlog add changed "Обновлен интерфейс пользователя"
chlog add deprecated "Устаревший метод login_v1"
chlog add security "Обновлены зависимости для устранения уязвимостей"
```

### Просмотр изменений

```bash
# Показать невыпущенные изменения
chlog show

# Показать все изменения (включая выпущенные)
chlog show --all

# Вывести в JSON формате
chlog show --format json

# Показать статистику
chlog stats
```

### Создание релиза

```bash
# Простой релиз
chlog release 1.0.0

# Релиз с заметками
chlog release 1.2.0 --notes "Крупное обновление с новым API"

# Релиз с git тегом
chlog release v1.3.0 --tag

# Релиз с полным набором параметров
chlog release 2.0.0 --notes "Полный рефакторинг" --tag
```

### Управление изменениями

```bash
# Удалить изменение по типу и тексту
chlog remove --type added --pattern "тестовая запись"

# Удалить по номеру
chlog remove --index 3

# Показать изменения перед удалением (интерактивный режим)
chlog remove
```

## ⚙️ Конфигурация

### Просмотр конфигурации

```bash
# Показать текущую конфигурацию
chlog config show
```

### Изменение путей

```bash
# Изменить путь к CHANGELOG файлу
chlog config update paths.changelog "docs/CHANGELOG.md"

# Изменить путь к файлу невыпущенных изменений
chlog config update paths.unreleased ".changes/unreleased.json"

# Изменить путь к папке релизов
chlog config update paths.releases ".changes/releases"
```

### Изменение настроек

```bash
# Включить/выключить автоматические backup'ы
chlog config update settings.auto_backup true
chlog config update settings.auto_backup false

# Изменить формат даты
chlog config update settings.date_format "%d.%m.%Y"

# Включить интеграцию с Git
chlog config update settings.git_integration true
```

## 📁 Структура файлов

После инициализации создается следующая структура:

```
ваш-проект/
├── CHANGELOG.md                    # Основной файл изменений
└── .changelog/                     # Скрытая папка с данными
    ├── config.json                # Конфигурация
    ├── unreleased.json            # Невыпущенные изменения (JSON)
    └── releases/                  # Папка с релизами
        ├── release_1.0.0.json
        ├── release_1.1.0.json
        └── release_2.0.0.json
```

### Формат CHANGELOG.md

```markdown
# Changelog - Мой Проект

Все значимые изменения в этом проекте документируются в этом файле.

## [Unreleased]

## [1.2.0] - 2024-01-15

Крупное обновление с новым API

### Added
- Новый эндпоинт /api/users (Иван Петров)
- Система кэширования (Анна Сидорова)

### Fixed
- Исправлена утечка памяти (Иван Петров)

## [1.1.0] - 2024-01-10

### Changed
- Обновлен интерфейс пользователя
```

### Формат unreleased.json

```json
{
  "project": "Мой Проект",
  "created": "2024-01-15T10:30:00",
  "last_modified": "2024-01-15T14:20:00",
  "changes": {
    "added": [
      {
        "id": "chg_20240115142000_1a2b",
        "description": "Новый эндпоинт /api/users",
        "timestamp": "2024-01-15T14:15:00",
        "author": "Иван Петров",
        "status": "pending"
      }
    ],
    "fixed": [
      {
        "id": "chg_20240115142500_3c4d",
        "description": "Исправлена утечка памяти",
        "timestamp": "2024-01-15T14:20:00",
        "author": "Иван Петров",
        "status": "pending"
      }
    ]
  },
  "metadata": {
    "total_changes": 2
  }
}
```

## 🔧 Расширенные возможности

### Работа с несколькими проектами

```bash
# Проект 1
cd /projects/api
chlog init --name "Backend API"
chlog add added "Новый эндпоинт"

# Проект 2
cd /projects/frontend
chlog init --name "Frontend App"
chlog add added "Новый компонент"

# Проект 3 с кастомными путями
cd /projects/docs
chlog --changelog "docs/HISTORY.md" --unreleased ".changes/pending.json" init
```

### Интеграция с Git

```bash
# Создать релиз с git тегом
chlog release 1.0.0 --tag

# Вручную добавить CHANGELOG в коммит
git add CHANGELOG.md
git commit -m "chore: обновлен CHANGELOG"
git push
```
## 🎯 Рабочий процесс (Best Practices)

### Стандартный рабочий процесс

```bash
# 1. Начинаем новую фичу
git checkout -b feature/new-auth

# 2. Работаем над изменениями...
# 3. Добавляем записи в CHANGELOG
chlog add added "Новая система аутентификации OAuth2"
chlog add changed "Обновлена конфигурация сервера"

# 4. Завершаем фичу
git add .
git commit -m "feat: добавлена аутентификация OAuth2"
git push origin feature/new-auth

# 5. После мержа в main создаем релиз
chlog release 1.5.0 --notes "Добавлена поддержка OAuth2" --tag

# 6. Публикуем изменения
git push --tags
```

### Работа в команде

```bash
# Разработчик 1 добавляет свои изменения
chlog add added "Новый модуль аналитики" --author "Анна Смирнова"

# Разработчик 2 добавляет свои изменения
chlog add security "Обновлены криптографические библиотеки" --author "Петр Иванов"

# Менеджер проверяет перед релизом
chlog stats
# Вывод:
# 📊 Статистика невыпущенных изменений:
# ----------------------------------------
#   added       :  1
#   security    :  1
# ----------------------------------------
#   Всего      :  2

# Создаем релиз
chlog release 2.0.0 --notes "Крупное обновление безопасности"
```

## 🐛 Устранение проблем

### Проблема: "Command not found"
```bash
# Проверьте что скрипт в PATH
which chlog

# Если нет, добавьте вручную
export PATH="/usr/local/bin:$PATH"
```

### Проблема: "Permission denied"
```bash
# Добавьте права на выполнение
chmod +x /usr/local/bin/chlog

# Или запускайте через Python
python3 /path/to/changelog.py --help
```

### Проблема: Ошибочная запись в CHANGELOG
```bash
# Удалить запись
chlog remove --pattern "ошибочная запись"

# Или отредактировать вручную
nano .changelog/unreleased.json
```

### Проблема: CHANGELOG не обновляется
```bash
# Проверьте пути
chlog config show

# Проверьте существование файлов
ls -la .changelog/
cat .changelog/unreleased.json
```

## 📚 Поддерживаемые типы изменений

| Тип | Описание | Пример |
|-----|----------|--------|
| **added** | Новая функциональность | `chlog add added "Новый эндпоинт /api/users"` |
| **changed** | Изменение существующей функциональности | `chlog add changed "Обновлен интерфейс"` |
| **deprecated** | Устаревшая функциональность | `chlog add deprecated "Старый метод API"` |
| **removed** | Удаленная функциональность | `chlog add removed "Удалена поддержка IE11"` |
| **fixed** | Исправление ошибки | `chlog add fixed "Исправлен баг с кэшем"` |
| **security** | Исправление уязвимости | `chlog add security "Обновлены зависимости"` |

## 🔗 Полезные ссылки

- [Keep a Changelog](https://keepachangelog.com/) - Формат, которому следует утилита
- [Semantic Versioning](https://semver.org/) - Семантическое версионирование
- [Conventional Commits](https://www.conventionalcommits.org/) - Стандарт коммитов

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE.

## 🌟 Благодарности

- Спасибо всем контрибьюторам, которые помогают улучшать этот инструмент
- Вдохновлено лучшими практиками из [Keep a Changelog](https://keepachangelog.com/)

---

**💡 Совет:** Добавьте алиасы в ваш shell для быстрого доступа:

```bash
# Добавьте в ~/.zshrc или ~/.bashrc
alias chlog-add='chlog add'
alias chlog-show='chlog show'
alias chlog-release='chlog release'
alias chlog-stats='chlog stats'
```

Теперь управление CHANGELOG стало проще и эффективнее! 🚀